name: example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

publish_to: none

dependencies:
  flutter:
    sdk: flutter

  dash: ^1.0.7
  # dash:
  #   path: ../dash/
  rxdart: ^0.26.0
  cupertino_icons: ^1.0.2
  analyzer: ^7.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.3
  dash_generator: ^1.0.8
  # dash_generator:
  #   path: ../dash_generator/

flutter:
  uses-material-design: true